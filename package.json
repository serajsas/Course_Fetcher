{
  "name": "course_fetcher",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "docker:build": "docker build -t course_fetcher .",
    "docker:clean-image": "docker rmi $(docker images --filter \"dangling=true\" -q --no-trunc)",
    "docker:start": "docker run --publish 8080:8080 course_fetcher",
    "start": "npx ts-node src/Server.ts",
    "debug": "nodemon src/Server.ts",
    "test": "mocha --require ts-node/register --timeout 10000 test/**/*.spec.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/chai": "^4.3.3",
    "@types/express": "^4.17.13",
    "@types/mocha": "^9.1.1",
    "@types/node": "^18.7.8",
    "@types/nodemon": "^1.19.2",
    "body-parser": "^1.20.0",
    "chai": "^4.3.6",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mocha": "^10.0.0",
    "nodemon": "^2.0.19",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
